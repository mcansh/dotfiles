# shortcuts
alias g="git"
alias ga="git add -A"
alias gp="git push"
alias gpf="git push -f"
alias gc="git commit -sam"
alias projects="cd Dropbox\ \(Personal\)/Sites/"

# Open a file in Atom Beta
# i.e 'open "atom-beta" .aliases'
#alias open="open -a"


# Hide/show all desktop icons (useful when presenting)
alias hidedesktop="defaults write com.apple.finder CreateDesktop -bool false && killall Finder"
alias showdesktop="defaults write com.apple.finder CreateDesktop -bool true && killall Finder"


# Lock the screen (when going AFK)
alias afk="/System/Library/CoreServices/Menu\ Extras/User.menu/Contents/Resources/CGSession -suspend"


# Clean up LaunchServices to remove duplicates in the “Open With” menu
alias appflush="/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user && killall Finder"


# software updates becuase laziness
alias update="softwareupdate -ia --verbose"

# update everything
#alias updateall="sudo softwareupdate -ia --verbose; brew update; brew upgrade --all; brew cleanup; sudo gem install -n /usr/local/bin; npm update -g; sudo gem update --system /usr/bin/update_rubygems; sudo gem update /usr/local/bin"
alias updateall="sudo softwareupdate -ia --verbose; brew update; brew upgrade --all; brew cleanup;"


# Merge PDF files
# Usage: "mergepdf -o output.pdf input{1,2,3}.pdf"
alias mergepdf="/System/Library/Automator/Combine\ PDF\ Pages.action/Contents/Resources/join.py"

# alias for aliases
alias makethisgohere="ln -s"

# compress stuff
alias zip="zip -r -X"
alias tar="tar -jcvf"

# make list actually list all
alias ls="ls -a"

# install gems on el cap / Sierra
alias geminstall="sudo gem install -n /usr/local/bin"
